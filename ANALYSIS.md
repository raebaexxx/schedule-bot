# ANALYSIS.md — анализ вариантов корректного парсинга PDF-расписания

> Дополнение к PLAN.md. Повод: 2 курс БТ-251 ЧТ — предметы ячейки склеились
> в один («лек. История России» + «Прикладная физкультура» без маркера типа).

## 1. Корень проблемы

Ячейка слота ЧТ 08:30 (БТ-251, 2 курс) содержит ДВА занятия, у второго нет
kind-маркера в PDF (физра, «Учебная практика» всегда без «пр.»/«лек.»):

```
y=451.9: лек. История России            ← kind-маркер есть
y=453.5: КУКСИН А.И. 210
y=455.0: с 03.09 по 19.11
   [зазор 13.4pt]
y=468.6: Прикладная физическая культура ← маркера НЕТ
y=470.0: ВАСИЛЬЕВ А.Г. 311
y=471.3: (Игровые виды спорта) 03.09
```

Сегментатор резал только по kind-маркеру в начале строки → безмаркерные
занятия приклеивались к предыдущим. Таких склеек было 6+ в JSON.

## 2. Ключевое наблюдение (проверено на всех 4 PDF)

Межстрочный зазор:
- внутри одного занятия: **0.1–5.5 pt** (перенос слов, подгруппы «1н-1п»)
- **граница занятий в одной ячейке: 5.7–14 pt** (13.4pt в кейсе, 5.9/7.0 в
  ячейке с 4 занятиями БК-251(у))
- граница ячеек/подписи: больше

Все известные склейки — зазор >5.5pt на границе; все ложные разрывы ≤5.5pt.

## 3. Варианты и решение

**Выбрано (реализовано): «kind ИЛИ зазор + не-сэндвич»**
Разбивать на новое занятие при kind-маркере ИЛИ (зазор >5.5pt И строка —
не сэндвич). Сэндвич-строка: ≤3 слов, все — фамилия/инициалы/«вирт»/«ауд.»
(приклеивается к предыдущему занятию как ФИО). Строки только из чисел —
мусор, отсекаются до сегментации. Занятие может иметь kind='' (физра,
практика) — в боте отображается «—» вместо типа.

**Отклонённые альтернативы:**
- «kind ИЛИ зазор» без вида строки: «Учебная практика» отрывается от
  ФИО-сэндвича, больше правил привязки сирот.
- OCR/визуальный разбор: текстовый слой качественный, проблема в логике
  склейки; OCR добавил бы ошибки в ФИО.
- pdfplumber/camelot: у выгрузки нет линий сетки, табличные детекторы
  бесполезны; pdfplumber даёт те же слова, что pdftotext -bbox.
- Ручная правка JSON: против философии «PDF — единственный эталон».

## 4. Правило (финал)

```
GAP_SPLIT = 5.5  # pt
new_block = kind_marker OR (gap > GAP_SPLIT AND not sandwich_line)
sandwich_line = 1..3 слов, все: SURNAME|INITIALS|вирт|ауд
lines of only digits 1-2 chars = мусор (skip до сегментации)
```

## 5. Статус (02.09)

639 → **669 занятий** (расклеились безмаркерные физра/практика).
Метод B: все подтверждены. Sanity: чист. Тесты: 40 OK
(669 = суммарно, kind='' допустим для практик).
